language: node_js

node_js:
    - "node"
    - "10"
    - "8"
    - "6"

sudo: false

git:
    submodules: false

before_script:
    echo $PATH
    - npm install
    - wget http://chromedriver.storage.googleapis.com/2.24/chromedriver_win32.zip
    - unzip chromedriver_win32.zip -d /home/travis/bin
    - export CHROME_BIN=chromium-browser

script:
    - npm test

after_success:
    - npm run report-coveralls
    - npm run report-codecov
